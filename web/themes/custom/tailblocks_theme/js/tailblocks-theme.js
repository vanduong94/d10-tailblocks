/**
 * @file
 * tailblocks_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.tailblocksTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
